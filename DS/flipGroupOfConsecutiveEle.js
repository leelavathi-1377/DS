const flipGroupOfZeroesOrOnes = (arr) => {
    let minimalGroup = 0 
    for(let i = 0 ; i < arr.length ; i++){
        
    }

}


const arr = [1, 1, 1, 0, 0]
console.log(flipGroupOfZeroesOrOnes(arra))